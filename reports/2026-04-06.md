## 1000Problems — April 6, 2026

### Commits

| Project | # | SHA | Description |
|---|---|---|---|
| ytcombinator | 1 | `88feb35` | Add password-gated dashboard access with login flow |
| ytcombinator | 2 | `1b353cf` | Dark/light theme, ES/EN toggle, keyword row expand + income panel, login page, kids keyword seeds |
| ytcombinator | 3 | `91c7e06` | Replace flat $5 CPM with category-aware revenue model + COPPA toggle |
| ytcombinator | 4 | `09a6ad2` | Enrich POST /api/keywords response with inline metrics; persist filter tab in URL |
| ytcombinator | 5 | `533580d` | Fix dashboard blank metrics after UTC midnight rollover |
| ytcombinator | 6 | `0a1004d` | Add task spec: region multipliers + portfolio valuation column |
| GitMCP | 1 | `3de004b` | Initial commit: GitMCP server with 14 git tools |
| GitMCP | 2 | `7685ed3` | Add filesystem tools: write, read, list, delete files within allowed paths |
| GitMCP | 3 | `c91caa9` | Remove redundant file I/O tools that caused repeated permission prompts |
| Vybe | 1 | `4e3ac16` | Add SPEC.md and CLAUDE.md for VybePM web app rebuild |

VoiceQ, KitchenInventory, RubberJoints-iOS — no changes.

### Assessment

ytcombinator had a heavy day — 6 commits, all production deploys green. The work covered login/auth middleware (Edge-compatible, HMAC-signed cookie), full dark/light theming, ES/EN i18n, category-aware CPM with a COPPA toggle, inline metrics on keyword add, a UTC midnight rollover fix, and a new task spec for region multipliers. That's a wide surface area for a single session. The UTC midnight fix is the riskiest item: it rewrote CTE logic across three separate endpoints to replace `CURRENT_DATE` with a `MAX(snapshot_date)` lookup. It needs to actually be verified at the next UTC rollover rather than assumed correct. The COPPA toggle is new product territory — 16 CPM benchmark rows, two mode flags — and it has no tests mentioned in the commits.

GitMCP pushed what appears to be a re-initialization of the repo (initial commit at 11 AM yesterday) followed by a filesystem tools addition and then a partial rollback of those same tools due to permission prompt issues. That pattern — add, then remove — suggests the filesystem feature isn't done, just parked. The "redundant" framing in the commit message is a flag: if the file I/O tools were removed because they caused permission prompts, the underlying path validation bug still exists in whatever tools remain.

Vybe has a spec and a CLAUDE.md but no code. VoiceQ, KitchenInventory, and RubberJoints are idle for at least another day. All three are at real risk of context rot — no recent commits means no working memory of what state they're in.

### Open Items

1. **ytcombinator UTC midnight fix** — verify the CTE rewrite actually resolves the blank metrics issue by checking the dashboard after 00:00 UTC tonight; don't assume the fix is correct without a real rollover test
2. **ytcombinator region multipliers** — task spec committed (`0a1004d`) but not implemented; Code needs to seed `us_es` (0.70x) and `latam_es` (0.25x) CPM rows and parameterize region in the API
3. **ytcombinator portfolio valuation column** — also in the spec; Annual Value column + portfolio valuation card on Starred view, no code yet
4. **GitMCP file I/O** — removing tools instead of fixing the dotfile/path permission issue is a workaround; the underlying validation bug should be addressed properly before the MCP is used as a dependency
5. **Vybe scaffold** — spec exists, nothing is deployed; needs a project scaffold and Vercel setup before it can be iterated on
6. **VoiceQ, KitchenInventory, RubberJoints** — three repos with no recent activity; do a quick status check on each to confirm they're not broken or blocked, and decide whether they're on hold intentionally
